# YourWork 开发原则

## 1. 大改动流程要求
所有大改动必须遵循以下流程：
- 先制定开发计划
- 计划需要经过审批
- 审批同意后才能开始开发

## 2. 测试要求
所有大改动必须包含完整的测试覆盖：
- 单元测试（test/unit/）
- 集成测试（test/integration/）
- 系统测试（test/system/）

## 3. 命名规则一致性
前后端开发语言命名规则必须保持一致：
- 统一使用 snake_case（蛇形命名法）
- 不使用 camelCase 或其他命名风格
- 变量、函数、文件名均遵循此规则

## 4. 依赖管理原则
- 优先使用原生代码/内置模块实现功能
- 尽量避免引入外部依赖库
- 必须使用外置库时，要满足以下条件：
  - 能够访问库的源代码
  - 了解其实现原理
  - 不是黑盒状态
- 只有在必须使用且无替代方案时，才允许引入黑盒库
